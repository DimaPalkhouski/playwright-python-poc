[pytest]
testpaths = tests
addopts =
    --html=reports/report.html
    --self-contained-html
    -v
markers =
    smoke: fast critical-path tests
    regression: full regression suite
    auth: authentication-related tests
    compliance: state-specific compliance rule validation
    unit: pure-python tests without a browser
